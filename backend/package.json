{
  "name": "backend",
  "private": true,
  "scripts": {
    "dev": "concurrently -n events,users -c blue,magenta \"npm run dev --prefix services/events-service\" \"npm run dev --prefix services/user-service\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
